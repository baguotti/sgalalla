/**
 * Shared map / stage configuration.
 * Re-exports from the shared module so all existing client imports continue to work.
 */
export { MapConfig, ZOOM_SETTINGS } from '../../shared/MapConfig.ts';
export type { ZoomLevel } from '../../shared/MapConfig.ts';
